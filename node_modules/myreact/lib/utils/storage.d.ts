export interface IAppStorageOptions {
    driver: string;
}
declare abstract class AppStorage {
    protected lf: LocalForage;
    protected keys: {
        userState: string;
    };
    constructor({driver}: IAppStorageOptions);
    getUserState: () => Promise<any>;
}
export default AppStorage;
